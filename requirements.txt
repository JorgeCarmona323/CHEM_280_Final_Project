# ── Python dependencies for CHEM 280 Final Project ───────────────────────────
#
# Preferred setup (reproducible):
#   conda env create -f environment.yml
#   conda activate chem280
#
# pip-only fallback (requires gcc — see README):
#   pip install -r requirements.txt
#
# External binaries (not pip/conda installable):
#   FoldMason : wget https://mmseqs.com/foldmason/foldmason-linux-avx2.tar.gz
#               OR  conda install -c bioconda foldmason
#   FoldSeek  : conda install -c bioconda foldseek  (NB03 only)
#   BindCraft : https://github.com/martinpacesa/BindCraft  (NB02b, Colab only)
#   Forge     : HuggingFace yk0/forge-e80  (NB02c, Colab only)
#   ProteinMPNN: https://github.com/dauparas/ProteinMPNN  (NB02b/c, Colab only)
#   Protenix  : https://github.com/bytedance/protenix  (NB02b/c, Colab only)

# Core scientific computing
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# IDP / structure tools (NB01)
idptools-starling>=2.0.0a3
MDAnalysis>=2.6
afrc>=0.3.7

# ML / embeddings
torch>=2.0
fair-esm>=2.0.0         # ESMFold + ESM2
transformers>=4.35

# Dimensionality reduction + clustering (NB03)
umap-learn>=0.5.4
hdbscan>=0.8.33
scikit-learn>=1.3

# Structure parsing
biopython>=1.81
biotite>=0.38

# Utilities
tqdm>=4.65
pyyaml>=6.0
